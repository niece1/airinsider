language: php

php:
  - 7.4

before_script:
  - composer self-update
  - composer install --no-interaction
  - cp .env.travis .env
  - php artisan key:generate

script:
  - ./vendor/bin/phpcs

services:
  - redis-server